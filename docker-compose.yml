services:
  backend:
    build:
      context: ./backend

    container_name: grievance_backend

    env_file:
      - ./backend/.env

    ports:
      - "8000:8000"

    command: uvicorn main:app --host 0.0.0.0 --port 8000
